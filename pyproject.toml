[project]
name = "shortr"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = [
    "-v",
    "--cov=app",
    "--cov-report=term-missing",
]
testpaths = ["tests"]